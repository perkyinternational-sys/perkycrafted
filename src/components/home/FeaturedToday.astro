---
interface Article {
  title: string;
  category: string;
  href: string;
}

interface Props {
  articles?: Article[];
}

const { articles = [] } = Astro.props;
---

{articles && articles.length > 0 && (
  <section class="home-section home-section--featured" aria-labelledby="featured-heading">
    <div class="container">
      <div class="section-heading">
        <h2 id="featured-heading">Featured Today</h2>
        <p>Fresh ideas, practical tools and useful recommendations worth exploring.</p>
      </div>
      <div class="content-grid content-grid--three">
        {articles.map((article) => (
          <article class="content-card">
            <p class="card-eyebrow">{article.category}</p>
            <h3><a href={article.href}>{article.title}</a></h3>
            <a class="card-link" href={article.href}>
              Read guide <span aria-hidden="true">→</span>
            </a>
          </article>
        ))}
      </div>
    </div>
  </section>
)}
